---
// INFO: This file contains the Footer component.
---

<script is:inline>
  function switchTheme() {
    const html = document.documentElement;
    const button = document.getElementById("themeToggleButton");

    // Toggle button logic
    if (html.classList.contains("dark")) {
      html.className = "light";
      button.innerHTML = "Theme: Light";
      localStorage.setItem("theme", "light");
    } else {
      html.className = "dark";
      button.innerHTML = "Theme: Dark";
      localStorage.setItem("theme", "dark");
    }
  }
</script>

<style>
  footer {
    background: var(--mantle);
    padding: 16px;
    flex-shrink: 1;
    text-align: center;
    font-family: var(--body);
    font-size: 0.75rem;
    color: var(--surface1);
  }

  footer a {
    color: var(--surface1);
  }

  #links {
    display: flex;
    justify-content: center;
    align-items: center;
  }

  #links a {
    display: flex;
    justify-content: center;
    align-items: center;
    text-decoration: none;
  }

  #links a i {
    margin: 0 4px;
    color: var(--text);
    font-size: 1.5rem;
  }

  #themeToggleButton {
    margin-left: var(--xs-px);
    padding-left: var(--xs-px);
    border-left: solid var(--overlay0) var(--border-normal);
    color: var(--blue);
    cursor: pointer;
  }
</style>

<footer>
  <div id="links">
    <a href="https://github.com/ch3rries" aria-label="GitHub"
      ><i class="fa-brands fa-github"></i></a
    >
    <a href="https://x.com/cherryponee" aria-label="X"
      ><i class="fa-brands fa-x-twitter"></i></a
    >
    <a href="https://reddit.com/u/XSuperOptical" aria-label="reddit"
      ><i class="fa-brands fa-reddit-alien"></i></a
    >
    <a id="themeToggleButton" onclick="switchTheme()">Theme: NaN</a>
  </div>
  <p style="margin-top: 8px;">
    Noah Hancock, 2024 - Powered by <a
      href="https://github.com/catppuccin/catppuccin">Catppuccin</a
    >
  </p>
</footer>

<script is:inline>
  const html = document.documentElement;
  const currentTheme = localStorage.getItem("theme");
  const button = document.getElementById("themeToggleButton");

  // No theme, add default
  if (!html.classList.contains("light", "dark")) {
    // Default to dark theme
    html.classList.add("dark");
  }

  // Theme from localStorage
  if (currentTheme == "dark") {
    html.className = "dark";
    button.innerHTML = "Theme: Dark";
  } else {
    html.className = "light";
    button.innerHTML = "Theme: Light";
  }
</script>
